# September24 operational repair evidence

The operational candidate synchronizes scanner/base/benchmark/held streaming subscriptions, separates entry rejections and processing timings, observes actual scheduler/dependency readiness, and preserves unread critical-log history. It has not been deployed.

`local_acceptance.json` binds the final runtime source, full artifact pack, combined operational run, independent review, unchanged active runtime and candidate freeze. The combined selection passed1,900 tests with one intentionally unavailable installed-history check. The separate full pack passed168 safety/regression cases,28 replay cases and20 semantic cases; these counts overlap other runs and are not additive.

`operational_source_acceptance_review.json`, `watchdog_independent_review.json`, `pipeline_diagnostics_independent_review.json` and `subscription_freeze_independent_review.json` record independent reviews. Historical author reports and initial failed development attempts are retained; later final receipts supersede them only within their stated scope. The historical cache snapshot addendum is explicitly superseded.

`composite_defect/assessment.json` and `composite_feature_defect_review.json` document a separately reproduced existing scalar-input bug. A proposed arithmetic correction was evaluated only on synthetic feature/alpha data; it was not applied and is not a full trading replay. The real seven-zero fallback can change decision inputs. It does not quantify September24 missed trades or profitability.

`unaccepted_cache_prototype/` contains non-collectable source/test copies and a reversible patch, not runtime code. The prototype was withdrawn because caching fallback results could suppress retry. `profile_baseline_assessment.json` qualifies reliable timing data and explicitly withholds invalid fine-grained cProfile results. No feature-speedup claim is made.

`active_runtime_preservation.json` records the restored source-edit incident and fresh unchanged container/cutoff evidence. The stricter initial read-only freeze check failed on the logger's relative output path; the successful check used exact canonical bytes and installed source from an isolated scratch working directory, retaining the production-write restriction.

The active September23 freeze remains authoritative. The separately verified candidate has no FROZEN_AT and does not authorize release or reset. Public reports summarize private immutable captures; private broker credentials, raw account payloads and live brain files are excluded.
