# Optional OpenHands layer for Intra

Use OpenHands only after the repo-native control plane is working.
Recommended use: queue tasks from GitHub issues, run them in Docker sandbox, and open PRs back to GitHub.
Do not let OpenHands become the source of truth; GitHub issues, PRs, CI, and artifacts remain canonical.
