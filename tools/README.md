This directory contains developer tools and one-off scripts. Pytest should never collect anything from here.

Guidelines:
- Do not name files with the pattern `test_*.py` or `*_test.py` in this folder.
- Prefer putting runnable helpers in `scripts/` instead.
